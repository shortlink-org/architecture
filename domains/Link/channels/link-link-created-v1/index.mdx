---
id: link.link.created.v1
name: link.link.created.v1
version: 1.0.0
summary: |
  Fanout topic carrying LinkCreated events from the link service.
owners:
  - link-platform
address: link.link.created.v1
protocols:
  - kafka
  - amqp
deliveryGuarantee: at-least-once
---

## Overview

Produced by [[service|LinkService]], consumed by [[service|MetadataService]] to trigger enrichment.

On Kafka this is a topic; on RabbitMQ the chart provisions a durable fanout exchange of the same name
(`ops/link/templates/rabbitmq/exchange/link.link.created.v1.yaml`), gated on `MQ_TYPE: rabbitmq`. Production runs
Kafka.

<NodeGraph />

## Provisioning

Declared as a Strimzi `KafkaTopic` in the `kafka` namespace by the chart of the service that publishes it:

| | |
|---|---|
| Partitions | 1 |
| Replicas | 3 |
| Retention | **2 hours** (`retention.ms: 7200000`) |
| Segment size | 30MB |

> [!WARNING]
> Two hours of retention is short for an event log. A consumer that is down longer than that does not catch up — it
> skips whatever expired. Replaying history is not available on this channel.
