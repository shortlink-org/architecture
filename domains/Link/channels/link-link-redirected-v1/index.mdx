---
id: link.link.redirected.v1
name: link.link.redirected.v1
version: 1.0.0
summary: |
  Fanout topic carrying LinkRedirected events from the proxy.
owners:
  - link-platform
address: link.link.redirected.v1
protocols:
  - kafka
  - amqp
deliveryGuarantee: at-least-once
---

## Overview

Produced by [[service|ProxyService]] after every served redirect. This is the highest-volume topic in the domain —
it fires once per visit, whereas the write-side topics fire once per link change.

<NodeGraph />

## Provisioning

Declared as a Strimzi `KafkaTopic` in the `kafka` namespace by the chart of the service that publishes it:

| | |
|---|---|
| Partitions | 1 |
| Replicas | 3 |
| Retention | **2 hours** (`retention.ms: 7200000`) |
| Segment size | 30MB |

> [!NOTE]
> Retention is two hours, so this channel is a transport rather than a log. Consumers are expected to be online;
> there is no history to replay from and no catch-up beyond that window.
