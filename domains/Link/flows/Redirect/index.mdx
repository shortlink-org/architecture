---
id: Redirect
name: "Follow a short link"
version: "1.0.0"
summary: "A visitor opens a short link; the proxy resolves the Kratos session, the link service applies the privacy model, and the visitor is redirected or given a 404."
owners:
  - link-platform
steps:
  - id: "visitor"
    title: "Visitor opens a short link"
    actor:
      name: "Visitor"
    next_step:
      id: "gateway"
      label: "GET https://shortlink.best/s/{hash}"

  - id: "gateway"
    title: "Istio external-gateway"
    summary: "HTTPRoute prefix /s on shortlink.best. No auth policy on this path — the proxy chart deploys none"
    externalSystem:
      name: "Istio Gateway"
      summary: "Gateway API entry point in the istio-ingress namespace"
      url: "https://gateway-api.sigs.k8s.io/"
    next_step:
      id: "proxy"
      label: "→ shortlink-link-proxy:3020"

  - id: "proxy"
    title: "Proxy Service"
    summary: "Reads the Kratos session to obtain a user id — empty when there is none"
    service:
      id: "ProxyService"
      version: "1.0.0"
    next_steps:
      - id: "kratos_session"
        label: "Resolve the session"

  - id: "kratos_session"
    title: "Kratos"
    summary: "GET /sessions/whoami — 200 gives the identity id, 401 means anonymous"
    service:
      id: "Kratos"
      version: "1.0.0"
    next_steps:
      - id: "cache"
        label: "Anonymous — cache is usable"
      - id: "link_service"
        label: "Authenticated — cache is bypassed"

  - id: "cache"
    title: "Redis cache"
    summary: "Positive and negative entries, keyed by hash alone"
    externalSystem:
      name: "Redis"
      summary: "Proxy cache — used only for requests without a user id"
      url: "https://redis.io/"
    next_steps:
      - id: "redirect"
        label: "Hit"
      - id: "link_service"
        label: "Miss"

  - id: "link_service"
    title: "Link Service"
    summary: "Get(hash, userID). Public link returns immediately; private link resolves the caller's email first"
    service:
      id: "LinkService"
      version: "1.0.0"
    next_steps:
      - id: "kratos_admin"
        label: "Private link — resolve the email"
      - id: "redirect"
        label: "Public link"

  - id: "kratos_admin"
    title: "Kratos Admin API"
    summary: "GET /admin/identities/{user_id} — traits.email is checked against the allowlist"
    externalSystem:
      name: "Kratos Admin API"
      summary: "Any failure here becomes ErrPermissionDenied, indistinguishable from 'not allowed'"
      url: "https://www.ory.sh/docs/kratos/reference/api"
    next_steps:
      - id: "redirect"
        label: "Email on the allowlist"
      - id: "not_found"
        label: "Anything else"

  - id: "not_found"
    title: "404 Not Found"
    summary: "ErrPermissionDenied is rendered as 404, so the link's existence stays hidden"
    actor:
      name: "Visitor"

  - id: "redirect"
    title: "Visitor is redirected"
    summary: "302 to the original URL — the response does not wait for the event"
    actor:
      name: "Visitor"
    next_step:
      id: "link_redirected"
      label: "Asynchronously"

  - id: "link_redirected"
    title: "LinkRedirected"
    message:
      id: "LinkRedirected"
      version: "1.0.0"
---

{/* The body of this page is generated by scripts/sync-docs.mjs. The catalog renders the
    structure it can verify and takes descriptions from the repositories that define them. */}

<NodeGraph />
