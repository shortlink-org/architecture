---
id: delivery-system
name: Delivery System
version: 1.0.0
summary: |
  The Package and Courier aggregates, the dispatch algorithm that pairs them, the gRPC API over both, and the
  tracking exposed back to the storefront.
scope: internal
owners:
  - shop-platform
services:
  - id: DeliveryService
  - id: DeliveryGraphQL
  - id: SupportService
containers:
  - id: delivery-postgres
  - id: delivery-redis
entities:
  - id: Package
  - id: Courier
relationships:
  - id: order-system
    label: accepts delivery requests from, and reports status to
  - id: osrm
    label: resolves routes and distances via
  - id: temporal
    label: runs delivery and courier workflows on
actors:
  - id: courier-actor
    name: Courier
    label: accepts assignments and confirms delivery
    direction: inbound
badges:
  - content: Rust
    backgroundColor: orange
    textColor: orange
  - content: gRPC
    backgroundColor: green
    textColor: green
  - content: Event driven
    backgroundColor: purple
    textColor: purple
---

{/* The body of this page is generated by scripts/sync-docs.mjs. The catalog renders the
    structure it can verify and takes descriptions from the repositories that define them. */}

<ContextDiagram />
<NodeGraph />
