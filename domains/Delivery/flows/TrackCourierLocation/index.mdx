---
id: TrackCourierLocation
name: "Report a courier's position"
version: "1.0.0"
summary: "Positions arrive over Kafka rather than the API, are cached with a TTL and appended to history, and are republished as a domain event nobody consumes."
owners:
  - shop-platform
steps:
  - id: "courier"
    title: "Courier moves"
    summary: "Emulated in non-production by CourierEmulationService, walking an OSRM polyline at a configurable speed"
    service:
      id: "CourierEmulationService"
      version: "1.0.0"
    next_steps:
      - id: "osrm"
        label: "Route between pickup and delivery"
      - id: "location_received"
        label: "Publish while moving"

  - id: "osrm"
    title: "OSRM"
    externalSystem:
      name: "OSRM"
      summary: "Self-hosted routing engine — turns the two addresses into a road-network route"
      url: "https://project-osrm.org/"

  - id: "location_received"
    title: "CourierLocationReceived"
    summary: "JSON, no protobuf contract — the Go value object as it stands"
    message:
      id: "CourierLocationReceived"
      version: "1.0.0"
    next_step:
      id: "delivery"
      label: "delivery.courier.location_received.v1"

  - id: "delivery"
    title: "Delivery Service"
    summary: "The location consumer — topic comes from KAFKA_LOCATION_TOPIC, not from the compiled-in default"
    service:
      id: "DeliveryService"
      version: "1.0.0"
    next_steps:
      - id: "redis"
        label: "Cache the current position"
      - id: "postgres"
        label: "Append to history"
      - id: "location_updated"
        label: "Republish"

  - id: "redis"
    title: "Delivery Redis"
    summary: "Current position with a TTL — this is what dispatch reads, not Postgres"
    container:
      id: "delivery-redis"
      version: "1.0.0"
    next_step:
      id: "dispatch"
      label: "Haversine distance term"

  - id: "postgres"
    title: "Delivery Postgres"
    summary: "courier_location_history"
    container:
      id: "delivery-postgres"
      version: "1.0.0"

  - id: "location_updated"
    title: "CourierLocationUpdated"
    summary: "Courier id, location, timestamps — the smallest payload in the domain because it is the most frequent"
    message:
      id: "CourierLocationUpdated"
      version: "1.0.0"
    next_step:
      id: "no_consumer"
      label: "delivery.courier.location.v1"

  - id: "no_consumer"
    title: "No consumer"
    custom:
      title: "Nothing subscribes"
      icon: "InboxIcon"
      color: "gray"
      type: "Topic"
      summary: "delivery.courier.location.v1 has no consumers; retention is 1 day, the shortest in the domain"

  - id: "dispatch"
    title: "Auto-assignment"
    summary: "Candidates are filtered on status, capacity and zone, then sorted by distance from the cached position"
    flow:
      id: "DeliverPackage"
      version: "1.0.0"
---

{/* The body of this page is generated by scripts/sync-docs.mjs. The catalog renders the
    structure it can verify and takes descriptions from the repositories that define them. */}

<NodeGraph />
