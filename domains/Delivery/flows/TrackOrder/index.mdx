---
id: TrackOrder
name: "Track an order"
version: "1.0.0"
summary: "A customer watches a delivery from the storefront: one snapshot, or a stream that pushes a new one on every state change."
owners:
  - shop-platform
steps:
  - id: "customer"
    title: "Customer opens an order"
    actor:
      name: "Customer"
    next_step:
      id: "ui"
      label: "Order page"

  - id: "ui"
    title: "Shop UI"
    service:
      id: "ShopUI"
      version: "1.0.0"
    next_step:
      id: "bff"
      label: "One federated query, order and tracking together"

  - id: "bff"
    title: "Shop BFF"
    summary: "Cosmo Router — composes the carts and delivery subgraphs"
    service:
      id: "ShopBFF"
      version: "1.0.0"
    next_step:
      id: "delivery_graphql"
      label: "delivery subgraph :4013"

  - id: "delivery_graphql"
    title: "Delivery GraphQL Subgraph"
    summary: "Lives in Delivery, composed into a graph owned by Shop — the one synchronous path between the boundaries"
    service:
      id: "DeliveryGraphQL"
      version: "1.0.0"
    next_steps:
      - id: "get_tracking"
        label: "Snapshot"
      - id: "subscribe_tracking"
        label: "Live"

  - id: "get_tracking"
    title: "GetOrderTracking"
    summary: "Keyed by OMS order id — the caller has never seen a package"
    message:
      id: "GetOrderTracking"
      version: "1.0.0"
    next_step:
      id: "delivery"
      label: "gRPC"

  - id: "subscribe_tracking"
    title: "SubscribeOrderTracking"
    summary: "The only server-streaming RPC in the catalogue"
    message:
      id: "SubscribeOrderTracking"
      version: "1.0.0"
    next_step:
      id: "delivery"
      label: "gRPC stream"

  - id: "delivery"
    title: "Delivery Service"
    summary: "Resolves the package by order_id and answers from its own state"
    service:
      id: "DeliveryService"
      version: "1.0.0"
    next_step:
      id: "pubsub"
      label: "Streams subscribe to the order's channel"

  - id: "pubsub"
    title: "Delivery Redis"
    summary: "Per-order pub/sub channel — the replica that handled the transition publishes, the replica holding the stream receives"
    container:
      id: "delivery-redis"
      version: "1.0.0"
    next_step:
      id: "transitions"
      label: "Fed by every package transition"

  - id: "transitions"
    title: "Package state changes"
    flow:
      id: "DeliverPackage"
      version: "1.0.0"
---

{/* The body of this page is generated by scripts/sync-docs.mjs. The catalog renders the
    structure it can verify and takes descriptions from the repositories that define them. */}

<NodeGraph />
