---
id: Session
name: Session
version: 1.0.0
identifier: id
summary: Browser-side proof of authentication, carried in the ory_kratos_session cookie.
owners:
  - auth-platform
properties:
  - name: id
    type: UUID
    required: true
    description: Session id. Becomes the `session_id` claim of the issued token.
  - name: identity
    type: Identity
    required: true
    description: The authenticated identity
    references: Identity
    referencesIdentifier: id
    relationType: hasOne
  - name: aal
    type: string
    required: true
    description: Authenticator assurance level reached by this session
    enum: ['aal1', 'aal2']
  - name: expires_at
    type: timestamp
    required: true
    description: Creation time plus the 720h lifespan
  - name: active
    type: boolean
    required: true
    description: Whether the session is still valid
---

{/* The body of this page is generated by scripts/sync-docs.mjs. The catalog renders the
    structure it can verify and takes descriptions from the repositories that define them. */}

<EntityPropertiesTable />
