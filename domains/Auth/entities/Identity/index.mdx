---
id: Identity
name: Identity
version: 1.0.0
identifier: id
aggregateRoot: true
summary: A person's account in Ory Kratos — an id plus traits validated against the identity schema.
owners:
  - auth-platform
properties:
  - name: id
    type: UUID
    required: true
    description: Kratos identity id. Becomes the `sub` and `identity_id` claim of every issued token.
  - name: traits.email
    type: string
    required: true
    description: E-Mail. The password identifier, the TOTP account name, and the channel used for verification and recovery.
  - name: traits.name.first
    type: string
    required: false
    description: First name
  - name: traits.name.last
    type: string
    required: false
    description: Last name
  - name: metadata_public
    type: object
    required: false
    description: Public metadata, copied verbatim into the `metadata` claim by the Oathkeeper id_token mutator.
---

{/* The body of this page is generated by scripts/sync-docs.mjs. The catalog renders the
    structure it can verify and takes descriptions from the repositories that define them. */}

<EntityPropertiesTable />
