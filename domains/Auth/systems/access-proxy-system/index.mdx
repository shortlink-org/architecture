---
id: access-proxy-system
name: Access Proxy System
version: 1.0.0
summary: |
  The edge of the Zero Trust path. Turns a browser session cookie into a short-lived JWT that internal services can
  verify without calling back to Kratos.
scope: internal
owners:
  - auth-platform
services:
  - id: Oathkeeper
entities:
  - id: AccessToken
relationships:
  - id: identity-system
    label: validates sessions via /sessions/whoami
actors:
  - id: visitor
    name: Visitor
    label: sends cookie-authenticated requests
    direction: inbound
badges:
  - content: Ory Oathkeeper
    backgroundColor: green
    textColor: green
---

{/* The body of this page is generated by scripts/sync-docs.mjs. The catalog renders the
    structure it can verify and takes descriptions from the repositories that define them. */}

<ContextDiagram />
<NodeGraph />
