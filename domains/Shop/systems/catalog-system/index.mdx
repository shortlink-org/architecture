---
id: catalog-system
name: Catalog System
version: 1.0.0
summary: |
  Goods and office reference data, the back office that edits them, and the adapter that exposes them to the
  federated graph.
scope: internal
owners:
  - shop-platform
services:
  - id: AdminService
  - id: AdminGraphQL
  - id: AdminUI
  - id: EmailSubscriptionService
containers:
  - id: shop-postgres
  - id: shop-redis
entities:
  - id: Good
  - id: Office
relationships:
  - id: storefront-system
    label: exposes goods into the federated graph of
actors:
  - id: back-office-user
    name: Back-office user
    label: creates and edits goods
    direction: inbound
badges:
  - content: Python
    backgroundColor: blue
    textColor: blue
  - content: CRUD
    backgroundColor: gray
    textColor: gray
---

{/* The body of this page is generated by scripts/sync-docs.mjs. The catalog renders the
    structure it can verify and takes descriptions from the repositories that define them. */}

<ContextDiagram />
<NodeGraph />
