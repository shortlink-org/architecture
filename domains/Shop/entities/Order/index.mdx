---
id: Order
name: Order
version: 1.0.0
identifier: order_id
aggregateRoot: true
summary: A committed purchase — items, customer, status, and the delivery information attached when it ships.
owners:
  - shop-platform
properties:
  - name: order_id
    type: UUID
    required: true
    description: Aggregate identifier.
  - name: customer_id
    type: UUID
    required: true
    description: Who placed the order.
  - name: items
    type: OrderItem[]
    required: true
    description: Good id, quantity and price per line, captured at checkout.
    references: Good
    referencesIdentifier: id
    relationType: hasMany
  - name: status
    type: OrderStatus
    required: true
    description: Lifecycle state of the order.
    enum: ['PENDING', 'PROCESSING', 'COMPLETED', 'CANCELLED']
  - name: delivery_status
    type: DeliveryStatus
    required: false
    description: Last delivery state heard from the Delivery boundary. Absent for orders that do not ship.
    enum: ['ACCEPTED', 'ASSIGNED', 'IN_TRANSIT', 'DELIVERED', 'NOT_DELIVERED']
  - name: package_id
    type: UUID
    required: false
    description: Identifier assigned by the Delivery boundary. A reference across a boundary, not a foreign key.
  - name: aggregate_version
    type: int32
    required: true
    description: Incremented per mutation. Carried on every event and used to reject stale delivery updates.
---

{/* The body of this page is generated by scripts/sync-docs.mjs. The catalog renders the
    structure it can verify and takes descriptions from the repositories that define them. */}

<EntityPropertiesTable />
