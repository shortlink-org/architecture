{
  "name": "architecture",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "eventcatalog dev",
    "editor": "npx @eventcatalog/editor@latest",
    "build": "eventcatalog build",
    "start": "eventcatalog start",
    "preview": "eventcatalog preview",
    "generate": "eventcatalog generate",
    "federate": "eventcatalog federate",
    "export": "eventcatalog export",
    "lint": "eventcatalog-linter",
    "test": "echo \"Error: no test specified\" && exit 1",
    "sync:schemas": "node scripts/sync-schemas.mjs",
    "check:schemas": "node scripts/sync-schemas.mjs --check"
  },
  "dependencies": {
    "@eventcatalog/core": "4.9.0",
    "@eventcatalog/linter": "1.1.17"
  }
}
