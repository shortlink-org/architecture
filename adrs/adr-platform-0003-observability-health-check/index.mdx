---
id: adr-platform-0003-observability-health-check
name: "ADR-0003 (platform): Observability"
version: 1.0.0
summary: We need to be able to monitor the health of our services and be able to collect data on the metrics we are most interested in a standardised way
status: accepted
date: 2022-10-28
owners:
  - platform
decisionMakers:
  - platform
appliesTo:
  - type: service
    id: LinkService
  - type: service
    id: MetadataService
  - type: service
    id: AuthService
badges:
  - content: Observability
    backgroundColor: green
    textColor: green
---

{/* Generated by scripts/sync-adrs.mjs from shortlink-org/shortlink/docs/ADR/decisions/0003-observability-health-check.md — do not edit here.
    Change the decision in its own repository, or adrs.overlay.yaml for the catalog links. */}

> Source: [docs/ADR/decisions/0003-observability-health-check.md](https://github.com/shortlink-org/shortlink/blob/main/docs/ADR/decisions/0003-observability-health-check.md)

## Context

We need to be able to monitor the health of our services and be able to collect data on the metrics we are most interested in a standardised way

## Decision

We will add three standard HTTP-endpoints to each service on port 9090.

- `/metrics` will return a prometheus formatted metrics page
- `/healthz` will return a json object with a `status` field that is either `ok` or `error`
- `/ready` will return a json object with a `status` field that is either `ok` or `error`

#### Dockerfile

We will add command `HEALTHCHECK` to the dockerfile for each service that will call the `/healthz` endpoint and check the status.

#### Helm

Also, we add k8s *-probes to each service that will call the `/healthz` endpoint and check the status.

For Helm-chart we use complete template:

```gotemplate
{{- include "shortlink-common.probe" .Values.deploy | indent 6 }}
```

## Consequences

We will be able to monitor the health of our services and collect metrics in a standardised way.
