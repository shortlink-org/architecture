---
id: istio-edge
name: Istio Edge
version: 1.0.0
summary: |
  The Gateway API edge. Two Istio gateways — one public, one internal — carry every request in the platform, and the
  Istio policies attached to each workload decide what is allowed through.
scope: internal
owners:
  - platform
relationships:
  - id: access-proxy-system
    label: routes /api to
  - id: identity-system
    label: routes /api/auth and /auth to
  - id: redirect-system
    label: routes /s to
  - id: link-management-system
    label: routes gRPC to
actors:
  - id: visitor
    name: Visitor
    label: reaches shortlink.best through Cloudflare
    direction: inbound
badges:
  - content: Istio
    backgroundColor: blue
    textColor: blue
  - content: Gateway API
    backgroundColor: green
    textColor: green
---

{/* The body of this page is generated by scripts/sync-docs.mjs. The catalog renders the
    structure it can verify and takes descriptions from the repositories that define them. */}

<ContextDiagram />
<NodeGraph />
